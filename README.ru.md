# Impuls

*Русский · [English](README.md)*

Impuls — нативная утилита для macOS. Она превращает область вокруг «чёлки»
MacBook — либо компактный язычок у верхней кромки других Mac — в локальный
центр действий, плеер, полку файлов, историю буфера обмена, заготовки,
календарь, переводчик и заметки.

В Impuls 1.4 появился штатный модуль **«Аккумулятор»**. Он показывает заряд,
состояние питания, расчётное время работы или зарядки и те электрические
показатели, которые фактически предоставляет macOS. На Mac без внутреннего
аккумулятора этот же модуль становится **«Питанием»**. Набор значений зависит от
модели Mac и версии macOS; недоступные аппаратные данные не угадываются.

> Публичные сборки пока подписаны ad-hoc и не нотаризованы Apple, поэтому первая
> установка по-прежнему требует ручного разрешения Gatekeeper. Начиная с 1.2.9,
> последующие версии устанавливаются внутри Impuls через подписанный канал;
> Developer ID после получения уберёт предупреждение при первой установке.

## Конфиденциальность

- нет аналитики, телеметрии, рекламы и идентификации устройства;
- нет сетевых подключений без явного разрешения на проверку обновлений или
  явного открытия выбранного музыкального веб-сервиса;
- проверка обращается только к GitHub Releases проекта Impuls;
- обновление скачивается и устанавливается только после действия пользователя;
- буфер обмена, заметки, заготовки, файлы и календарь не отправляются с Mac;
- скрытые записи менеджеров паролей не попадают в историю;
- нет private MediaRemote API, запуска `/usr/bin/perl` и внедрения библиотек;
  обновление использует только документированные подписанные компоненты Sparkle.

Точные границы описаны в [PRIVACY.md](PRIVACY.md) и [SECURITY.md](SECURITY.md).

## Impuls Actions

В Impuls 1.2 появился единый локальный поиск по истории буфера, заготовкам и
заметкам. Глобальная горячая клавиша сразу открывает Actions; стрелки вверх и
вниз выбирают результат, `Enter` копирует его, а панель команд позволяет
сохранить текст в заготовки, создать заметку, перевести текст, открыть ссылку
или файл и показать файл в Finder. Индекс строится из действующих локальных
хранилищ, не отправляется в сеть и не сохраняется как отдельная база данных.

При управлении мышью один клик выбирает результат без выполнения команды, а
двойной клик копирует его. Наведение только подсвечивает строку, поэтому при
движении к панели команд пересекаемые результаты больше не подменяют явный выбор.

В Impuls 1.2.1 добавлены закрепление записей, контекстные действия для ссылок,
адресов электронной почты, телефонов и JSON, пауза мониторинга, срок хранения и
исключения для выбранных приложений. По умолчанию история по-прежнему остаётся
только в памяти. Опциональное сохранение между запусками шифрует локальный архив
с помощью AES-GCM, а случайный ключ хранит в системной Связке ключей macOS.

## Файловые инструменты

В Impuls 1.2.2 на «Полке» появились локальные файловые операции. Изображения
можно конвертировать в PNG, JPEG или HEIC, уменьшать до выбранного размера,
объединять в многостраничный PDF, распознавать на них текст и удалять фон через
Apple Vision. Также доступны AirDrop, системное меню «Поделиться», копирование
пути и безопасное переименование. Любое преобразование создаёт рядом с
исходником новый файл с уникальным именем; оригинал не перезаписывается и не
отправляется в сеть.

В Impuls 1.2.3 эти операции работают и с группой выделенных изображений. Для
долгих серий показывается прогресс, доступен системный «Быстрый просмотр», а
последнее создание файлов или переименование можно безопасно отменить. Impuls
перемещает созданные результаты в Корзину только если они не изменялись после
операции; изменённый пользователем файл приложение не трогает.

## Стабильность и обратная связь

В Impuls 1.2.9 добавлены защищённые встроенные обновления через Sparkle 2.9.5.
После согласия приложение проверяет фиксированный HTTPS-канал, валидирует подпись
feed и Ed25519-подпись архива до распаковки, заменяет приложение, перезапускает
его и не оставляет установщик в «Загрузках». Тихая установка и отправка
системного профиля отключены.

Impuls 1.2.8 разделяет наведение, выбор и выполнение команды в Actions. Нижняя
панель теперь остаётся связана с явно выбранным результатом, пока курсор движется
через список; один клик выбирает строку, двойной — копирует её.

Impuls 1.2.7 удерживает открытую наведением панель на всё время работы
контекстного меню, включая вложенные инструменты для изображений. После закрытия
меню снова действует обычная логика наведения — без оторванных подменю и
визуальных артефактов.

Стабилизационные улучшения 1.2.6 сохраняются: строки интерфейса и локальный поиск
используют ограниченные текстовые представления, запись скриншотов и заметок
вынесена с UI-потока, запросы к плеерам объединяются, обложки уменьшаются до
экранного размера, а поиск ссылок в календаре имеет явный вычислительный лимит.
Перед отменой файловой операции Impuls теперь сверяет SHA-256 содержимого.

Из меню или настроек можно открыть центр обратной связи: сообщить о проблеме,
предложить улучшение или оставить общую оценку. Отчёт формируется локально и
показывается до отправки. Технические сведения опциональны и ограничены версией
Impuls, macOS и архитектурой процессора. Содержимое буфера, заметки, файлы,
пути, календарь, логи и идентификаторы устройства не добавляются. Impuls сам
ничего не загружает — только по явному действию копирует отчёт и открывает
публичную форму GitHub в системном браузере.

Формы репозитория используют те же ключевые поля — раздел, частоту, оценку и
версию; свободные неструктурированные issues отключены. Это делает добровольные
обращения пригодными для приоритизации без добавления аналитики в приложение.

## Настройки и управление с клавиатуры

В нативном окне настроек можно выбрать
глобальную горячую клавишу, режим наведения, размер панели, рабочий экран,
видимые модули и их порядок. После открытия с клавиатуры стрелки влево и вправо
переключают модули, а `Esc` закрывает панель. Настройки, заготовки и заметки
можно экспортировать в локальную JSON-копию и восстановить из неё.

## Аккумулятор и питание

На MacBook модуль «Аккумулятор» получает через публичный IOPowerSources заряд,
состояние зарядки, расчётное время до разрядки или полной зарядки, ток,
напряжение, температуру — если система её публикует — и номинал адаптера.
Мощность на стороне аккумулятора и номинал адаптера намеренно показаны раздельно:
адаптер 70 Вт не означает, что батарея прямо сейчас получает 70 Вт. Счётчик
циклов локально читается через небольшой fallback публичного IORegistry, только
когда Mac действительно публикует это значение. Данные питания не сохраняются и
никуда не отправляются.

MagSafe и USB-C показываются только при наличии надёжного источника. Публичный
API адаптера описывает сам адаптер, но не активный порт зарядки, поэтому в иных
случаях разъём остаётся неопределённым. На Mac mini, Mac Studio, iMac и Mac Pro
модуль показывает доступный системный источник питания, не придумывая мощность
потребления от сети.

## Управление музыкой

Источник теперь явно выбирается в самой вкладке «Музыка». Нативный адаптер
Apple Music использует сценарный интерфейс и события плеера; из музыкальных
системных прав ему нужна только «Автоматизация». Для России первым веб-источником
предлагается Яндекс Музыка, также доступны VK Музыка и YouTube Music.
Их официальные сайты открываются в отдельном системном окне WebKit только после
кнопки «Открыть веб-плеер». После этого панель у «чёлки» показывает ограниченный
набор метаданных страницы и передаёт ей команды управления и перемотки. Сам
выбор источника сетевой запрос не запускает.

Spotify в списке нет намеренно: его веб-плеер расшифровывает звук через
Widevine, которого в WebKit нет, поэтому встроенная вкладка авторизуется, но
никогда не играет.

Impuls не использует неофициальные API каталогов, не копирует аудио, не обходит
подписку или DRM и не обращается к закрытой системной базе Now Playing.

## Требования

- macOS 15 или новее;
- Swift 6 для сборки из исходного кода.

На Mac без физической «чёлки» используется язычок 96 × 10 pt и зона наведения
120 × 16 pt. На MacBook геометрия определяется по safe area дисплея.

## Сборка

```bash
swift test -c release
./Scripts/bundle.sh release
open build/Impuls.app
```

При наличии переменной `IMPULS_DEVELOPER_ID_APPLICATION` скрипт использует
Developer ID. Без неё создаётся ad-hoc сборка, которой на другом Mac потребуется
ручное разрешение Gatekeeper.

## Обновления

При первом запуске Impuls один раз спрашивает разрешение проверять GitHub
Releases. При выборе «Не проверять» запросы обновления не выполняются. Решение
можно изменить в меню. Версия 1.2.9 устанавливается вручную последний раз.
Начиная с 1.2.10 команда «Проверить обновления…» скачивает
подписанный ZIP во временное системное хранилище, проверяет его до распаковки,
заменяет Impuls, перезапускает приложение и удаляет временные файлы. Developer ID
и нотаризация всё ещё нужны, чтобы убрать Gatekeeper при первой установке на Mac.

## Лицензия и происхождение

Impuls основан на `akalikbergenov/cyclop`. Уведомление об исходной MIT-лицензии
сохранено в [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md). Новые изменения
Impuls распространяются по GPL-3.0-or-later. Лицензия кода не предоставляет прав
на название и фирменное оформление Impuls; см. [TRADEMARKS.md](TRADEMARKS.md).
