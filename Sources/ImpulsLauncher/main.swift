import ImpulsCore

// Keep the system's parameterized shortcut catalog in sync with the static
// first-wave definitions before the normal Impuls composition root starts.
ImpulsAppShortcuts.updateAppShortcutParameters()
ImpulsApplication.run()
